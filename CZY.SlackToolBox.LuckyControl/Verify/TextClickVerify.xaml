<UserControl x:Class="CZY.SlackToolBox.LuckyControl.Verify.TextClickVerify"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CZY.SlackToolBox.LuckyControl.Verify"
             xmlns:icon="clr-namespace:CZY.SlackToolBox.LuckyControl.IconResource"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Canvas x:Name="PART_Canvas">

        </Canvas>
        <TextBlock x:Name="PART_Info" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Button x:Name="PART_Reset" Grid.Row="1" Visibility="Collapsed"  >
            <icon:IconFont IconName="Reset" Foreground="Gray" Width="16" Height="16"></icon:IconFont>
        </Button>
    </Grid>
</UserControl>
